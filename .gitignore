# Local-only files — never publish
Profile.pdf
*.pdf
_to_delete/
.DS_Store
Thumbs.db
desktop.ini

# Secrets — never publish (defense in depth; none should exist in this repo)
.env
.env.*
*.pem
*.key
*credentials*
*secret*
